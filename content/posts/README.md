# Future blog posts

This folder is reserved for the future publishing workflow.

Phase 1 uses `DATA.posts` in `js/site.js` so publishing stays simple. A later phase can migrate this folder to Markdown/Jekyll posts without changing the visual design.