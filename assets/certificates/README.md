# Certificate images

Add certificate images/PDF preview images here.

For each certificate, keep the title, issuer, date and official verification URL in `js/site.js`. This keeps the page easy to update without redesigning the layout.