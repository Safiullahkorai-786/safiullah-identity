# Gallery images

Add personal, event, conference and milestone photos here.

Recommended naming:
- `event-name-year.jpg`
- `milestone-name.jpg`
- `conference-name.jpg`

Then add the image to `gallery.html` or, in a later phase, move gallery entries into `js/site.js` so the page can render them automatically.